language: node_js
node_js:
    - "6"
    - "7"
    - "8"
cache:
    directories:
        - node_modules
after_script: npm run coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
